A script int Java that completes the union of two sets. 

This will take the input of setA and setB and return a new set containing 
the elements that are present in either set leaving out duplicates.
